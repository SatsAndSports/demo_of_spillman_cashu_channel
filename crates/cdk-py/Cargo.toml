[package]
name = "cdk-py"
version = "0.1.0"
edition = "2021"
description = "Python bindings for Cashu payment channels"

[lib]
name = "cdk_py"
crate-type = ["cdylib"]

[dependencies]
cdk = { path = "../cdk", default-features = false }
pyo3 = { version = "0.22", features = ["extension-module"] }
